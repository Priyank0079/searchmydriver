import dotenv from 'dotenv'; dotenv.config(); import mongoose from 'mongoose'; import { estimateFareService } from './src/services/pricing.service.js'; mongoose.connect(process.env.MONGO_URI, { dbName: process.env.DB_NAME }).then(async () => { try { const res = await estimateFareService({ serviceType: 'hourly', bookedHours: 2 }); console.log(res); } catch (e) { console.error(e); } process.exit(0); });
