import dotenv from 'dotenv'; dotenv.config(); import mongoose from 'mongoose'; import jwt from 'jsonwebtoken'; import User from './src/models/user.model.js'; mongoose.connect(process.env.MONGO_URI, { dbName: process.env.DB_NAME }).then(async () => { const user = await User.findOne(); if (!user) { console.log('no user'); process.exit(0); } const token = jwt.sign({ id: user._id, role: 'user' }, process.env.ACCESS_TOKEN_SECRET, { expiresIn: '1h' }); console.log(token); process.exit(0); });
