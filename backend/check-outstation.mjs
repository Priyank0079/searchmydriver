import dotenv from 'dotenv'; dotenv.config(); import mongoose from 'mongoose'; import { getServicePricingByTypeService } from './src/services/pricing.service.js'; mongoose.connect(process.env.MONGO_URI, { dbName: process.env.DB_NAME }).then(async () => { try { const p = await getServicePricingByTypeService('outstation'); console.log('Outstation pricing:', p ? p.isActive : 'null'); } catch(e) { console.error(e.message); } process.exit(0); });
